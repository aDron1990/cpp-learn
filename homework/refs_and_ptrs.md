# Задача 1. Ссылка vs указатель

Напиши две функции:

```cpp
void increment_ref(int& x);
void increment_ptr(int* x);
```

Обе функции должны увеличивать переданное число на 1.

# Задача 2. Возврат ссылки из функции

Напиши функцию:

```cpp
int& max_ref(int& a, int& b);
```

Функция должна возвращать ссылку на больший из двух аргументов. Вывести x и y

# Задача 3. Константность ссылок и указателей

Даны объявления:

```cpp
int a = 10;
int b = 20;

const int& ref = a;
const int* ptr1 = &a;
int* const ptr2 = &a;
```

Требуется:
- Попробовать изменить a и b через каждую из переменных
- Попробовать изменить адрес, на который указывает ptr1 и ptr2
- Закомментировать строки, которые не компилируются, и объяснить почему

# Задача 4. Указатель на указатель


Напиши функцию:

```cpp
void allocate(int** ptr);
```

Функция должна:
- Выделять память под int (new int)
- Записывать значение 42